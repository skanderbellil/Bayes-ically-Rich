# Experiments

Reproducible studies that wire the `posterioralpha` pipeline stages together.
Each script loads data, runs one or more backtests, prints a metrics table, and
writes plots/CSVs to `results/`.

Run from anywhere — `_bootstrap.py` puts the repo root on `sys.path`:

```bash
python experiments/run_real_only.py
```

| script | universe | what it studies |
|--------|----------|-----------------|
| `build_etf_universe.py` | ~1,300 → top-250 ETFs | **Builds the large ETF universe.** financedatabase (universe + info) + yfinance (history), screened for coverage & dollar-volume liquidity → `datasets/etf_universe_*`. Network; run once. |
| `build_equity_universe.py` | ~820 → top-500 equities | **Builds the large equity universe.** US large/mega-cap names from financedatabase + yfinance, same liquidity screen → `datasets/equity_universe_*`. Network; run once. ⚠️ current-membership universe → survivorship-biased history. |
| `build_factor_data.py` | external factor data | **Builds the free factor datasets cited in the knowledge base** (`--source ff\|jkp\|openap\|all`): Kenneth French US FF5+Mom monthly+daily, Europe 5F+Mom, 12 industries → `datasets/ff_*`; Jensen-Kelly-Pedersen 13 theme returns × 4 regions (1926→) → `datasets/jkp_factors.csv.gz`; Chen-Zimmermann 212 predictor L/S returns + signal docs → `datasets/openap_*` (needs `pip install -e .[data]` for gdown). Unblocks FF5+UMD alpha attribution of any strategy, cross-region validation, and factor-momentum research. Network; run once. |
| `run_large_universe.py` | 250 liquid US ETFs | Universe summary from fd info + diversified cross-asset allocation on a basket built from that info (most-liquid ETF per asset class) vs SPY. Offline. |
| `run_equity_cross_section.py` | 500 liquid US equities | Exploratory 12-1 cross-sectional momentum (monthly decile L/S) vs SPY, gross/net of turnover cost. Offline. ⚠️ survivorship-biased universe. |
| `main.py` | S&P 500 (live download) | Multi-seed robustness of the Bayesian family vs. SPY; saves dashboards, multi-seed bars, λ heatmap |
| `run_local.py` | 5 ETFs + 9 synthetic = 14 | Full Bayesian × AMR comparison on an expanded factor-model universe |
| `run_real_only.py` | 5 real ETFs | Headline comparison on real data only — no synthetic assets |
| `run_refined.py` | 14 (synthetic-expanded) | Newer models only (`bocpd_amr`, `hmm3_amr`) over a shorter, faster window |
| `run_bocpd_v2.py` | 5 real ETFs | BOCPD-AMR v2: multi-asset BOCPD + CVaR + low-vol tilt + adaptive leverage |
| `run_bocpd_v3.py` | 5 real ETFs | BOCPD-AMR v3: continuous λ via the Omega ratio |
| `run_bocpd_v4.py` | 5 real ETFs | BOCPD-AMR v4: ERL-adaptive EWMA halflife for Omega |
| `run_spy_timing.py` | SPY only | Can a simple risk-on/risk-off rule on SPY beat buy-and-hold? |
| `run_market_neutral.py` | 5 real ETFs | Beta-hedged residual mean-reversion (market-neutral alpha sleeve) with BOCPD gating + vol targeting |
| `run_hurst_multiasset.py` | 5 real ETFs | Cross-sectional Hurst (R/S) trend ranking, top-K inverse-vol with IVOL-5 fallback (IS/OOS split) |
| `run_hurst_bull_adaptive.py` | SPY only | Hurst-bull SPY timing with adaptive (rolling-quantile) H/ERL thresholds + walk-forward |
| `run_intramonth_momentum.py` | 5 real ETFs | Nathan-Suominen-Tasa (2026): does WML momentum concentrate in the T-9→T-4 intramonth window? |
| `run_intramonth_bayesian.py` | 5 real ETFs | Learns the intramonth allocation per T-k position via an online Normal-Normal model (no hardcoded window) |
| `run_polymarket_momentum.py` | Polymarket markets (live) | **Prediction-market** cross-market momentum in log-odds space: xs momentum vs its contrarian mirror, time-series momentum, and a long-all baseline — Bayesian-shrunk, cost-aware, no lookahead. Pulls live Gamma + CLOB data (cached under `data/raw/polymarket/`); `--refresh` re-pulls. First finding: weekly cross-section mean-reverts (favorite–longshot), weak (\|Sharpe\|≈0.18), dies to 50 bps cost. See `docs/polymarket/`. |
| `run_polymarket_vol_outcome.py` | Polymarket markets (live) | **Event study**: does a sharp upside vol move predict the *actual* resolution? Buckets `(market,day)` episodes by upside log-odds vol / `sharp_up` / `vol_skew` / BOCPD changepoint prob and reports actual Yes-rate, calibration residual vs price, and forward drift. Finding: sharp up-moves are mildly informative about the outcome (markets underprice Yes) but the sharpest spikes overreact short-term (forward drift flips negative). `--by` picks the metric; `--refresh` re-pulls. See `docs/polymarket/SHARP_MOVE_OUTCOME.md`. |
| `run_polymarket_event_trades.py` | Polymarket markets (live) | **Harvesting the sharp-move edge**: event-driven trades (fill *after* the spike, slippage per crossing, one position/market) comparing exit policies. Idea 1 (hold Q4/Q5 Yes to resolution) earns ~+5¢ per $1 contract and survives 2% slippage; Idea 2 (trade the drift via take-profit / fixed horizon) breaks even or loses to the short-term overreaction. `--metric` / `--slippage` / `--min-price` / `--max-price` configurable. ⚠️ source of the PnL corrected by the band-control study below. See `docs/polymarket/EVENT_TRADE_HARVEST.md`. |
| `run_polymarket_band_control.py` | Polymarket markets (live) | **Level control / correction**: within fixed price bands, compares high- vs low-vol episodes to test whether the sharp-move edge is a real vol signal or a price-level artifact. Finding: the hold-to-resolution PnL is the favorite–longshot calibration of the entry band (a vol filter slightly *hurts*); the level-controlled vol signal sign-flips (longshots revert, favorites continue +29% Yes) and is already priced. Self-correcting result. See `docs/polymarket/BAND_CONTROL.md`. |
| `run_polymarket_universe_robustness.py` | Polymarket markets (live) | **Robustness**: pulls a larger 514-market universe, tags each by topic (`categorize.py`), and re-runs the [0.15,0.30] calibration per category. Finding: the favorite–longshot underpricing is **political, not structural** — +42% politics, +22% geopolitics, but ≈0 in sports (largest category) and crypto. `--n-markets` / `--min-volume`. See `docs/polymarket/UNIVERSE_ROBUSTNESS.md`. |
| `run_polymarket_politics_deep.py` | Polymarket markets (live) | **Stress-testing the political lead**: event-clustered calibration curve + a tradable long-underpriced / short-overpriced test with per-event bootstrap CIs. Finding: politics is genuinely mis-calibrated (underdogs underpriced, favorites overpriced) and diverse (25 events, persists 2024–25) — but traded honestly the per-event CIs span zero, it's indistinguishable from the non-political baseline, and it decays in 2026. Real mis-calibration, unproven edge. See `docs/polymarket/POLITICS_DEEP.md`. |
| `run_polymarket_efficiency.py` | Polymarket markets (live) | **Information efficiency** (descriptive, not an edge hunt): Brier score of the price over each market's life. Prices converge (Brier 0.081→0.028 into the last 3 days) and efficiency varies hugely by domain — Brier-skill-vs-base-rate +84% macro, +39% sports, but +4.5% geopolitics (near coin-flips). The inefficient domains are the mis-calibrated ones; their inefficiency is mostly irreducible uncertainty, not exploitable mispricing. See `docs/polymarket/EFFICIENCY.md`. |
| `dfl_orchestrator.py` | S&P 100 + ETFs | Decision-focused multi-pod orchestrator: 6 asset-level pods + a differentiable mean-variance QP layer. Requires `pip install -e .[dfl]` |
| `run_net_liquidity.py` | SPY/TLT + FRED | Net liquidity (Fed assets − TGA − RRP) vs equities: horizon lead-lag scan + three liquidity-driven strategies (binary on/off, scaled exposure, SPY↔TLT rotation) vs SPY. Offline (cached); `--build` refreshes FRED (needs `FRED_API_KEY`). |
| `run_gamma_exposure.py` | SPX/SPY options | Dealer GEX snapshot via the CBOE CDN (exchange greeks + OI, no key; SPX default) or yfinance: total gamma, long/short-gamma regime, zero-gamma flip, per-strike profile. Network. `--log` appends to `datasets/gex_snapshots.csv` (run daily to accumulate a GEX history, since free historical OI is scarce). |
| `run_net_liquidity_universe.py` | 250 ETFs + 500 equities + FRED | Where the net-liquidity effect lives: per-asset & per-asset-class sensitivity (ETF universe) + per-sector sensitivity (equity universe), most/least-sensitive names, and an out-of-sample regime-spread check. Offline. |
| `run_liquidity_trend.py` | SPY + FRED | Net liquidity × 200-day price trend: each signal alone, an AND gate, and a vote (0/½/1 exposure). The vote blend tops both standalone signals and SPY on Sharpe/Sortino. Offline. |
| `run_liquidity_predictive.py` | SPY + FRED + VIX/HYG | Net liquidity × *forward-looking* layers (VIX term structure, HYG−IEF credit appetite) instead of the lagging MA: pairwise and three-way votes vs the trend baseline and SPY. Best blend of the thread (3-way vote Sharpe 0.75, DD −20%). Offline. |
| `run_liquidity_continuous.py` | SPY + cyclical basket | Continuous fit-free vote (soft-sign strengths, no thresholds) on SPY and the XLY/XLK/XLF/XLE/XLI sleeve, with rolling 3y OOS win rates and per-year edge table. Best drawdown/Calmar of the thread; overlay beats its underlying in 60–68% of windows. Offline. |
| `run_liquidity_levered.py` | SPY + QQQ | Levering the vote for absolute return: four exposure mappings (vol-target, 2×vote, convex, vol-braked) with honest financing/TC. 2×vote on QQQ beats SPY's CAGR in 84% of 3y windows (median +8.4pp/yr) and QQQ's in 78% — at −48% max DD. Offline. Uses `research.overlay.liquidity_vote`. |
| `run_liquidity_4layer.py` | QQQ + FRED + VIX/UUP + 500 equities | Fourth vote layer head-to-head against the 2022 grinding-bear hole: dollar (−UUP 63d) vs equity breadth. Dollar wins — cuts 2022 loss and max DD, lifts the rolling CAGR win rate vs SPY to 88% with no Sharpe loss; breadth too slow, rejected. Offline. |
| `run_liquidity_stress.py` | QQQ/SPY/EEM + cyclicals, 2007→ | Stress-tests the 4-layer 2×vote: GFC-inclusive sample (live 2008-04, −8% through Sep08–Jun09 vs SPY −27%; survives the one non-QE-rescue crisis) passes; diversifying the underlying to a QQQ+EEM+cyclicals basket fails (deeper DD, lower return). Offline. |
| `run_liquidity_interaction.py` | SPY + FRED + VIX | Is liquidity state-dependent? corr(Δ6m liq, next-6m SPY) by causal VIX-percentile regime: +0.34 fragile vs −0.11 calm — a real conditional structure, but a naive fragile-only switch still trails buy & hold. Actionable as signal *weighting*, not a switch. Offline. |
| `run_liquidity_regime_weighted.py` | QQQ + FRED + VIX/UUP | Cashes in the interaction finding: liquidity layer weight = fragility rank inside the 4-layer 2×vote. Centered (w_liq = 2f) nudges the champion to its best OOS scores (90% CAGR / 74% Sharpe win vs SPY, Calmar 0.51→0.53); calm-mute (w_liq = f) is worse. Marginal refinement, not a breakthrough — 2022 unchanged. Offline. |
| `run_liquidity_disagreement.py` | QQQ + FRED + VIX/UUP | Cross-layer *disagreement* (std of the 4 strengths) as a risk gauge and throttle. Nearly VIX-orthogonal, yet high d cuts fwd 6m return +13%→+6%, fattens the 5th-pct tail +1%→−23% and survives a VIX-held-fixed control. Throttling the 2×vote by confidence crushes 2022 (−39%→−8%) and DD (−42%→−18%) at preserved Sharpe; the causally renormalized version recovers champion CAGR with the thread's best OOS consistency (97% CAGR-win vs SPY). Offline. |
| `run_model_disagreement.py` | QQQ + FRED + VIX/UUP | Mines the strategy graveyard: 5 model stances (liquidity vote, trend, mean-reversion, BOCPD regime age, walk-forward HMM) → cross-MODEL dispersion as a "model-implied uncertainty" gauge. **Hypothesis inverts**: high disagreement = strong trends = *good* times (fwd 6m +13% vs +8%, thinner tail), and 200d-MA flips follow trend/meanrev *agreement* (28% vs 7%) — near transitions all models drift to neutral, so dispersion is low. Indecision, not conflict, is the tell. Agreement-throttles fail; the post-hoc inverted throttle (2022 −5%, DD −21%, Sharpe 0.83) is flagged as unvalidated. Offline. |
| `run_overnight_split.py` | QQQ/SPY OHLC + FRED + VIX/UUP | Splits close-to-close into overnight (close→open) and intraday (open→close). The anomaly holds (QQQ overnight-only Sharpe 0.70 vs 24h 0.37, DD −34% vs −83%) and the sharp hypothesis **confirms: liquidity loads on the overnight leg** (corr +0.19 vs +0.03 intraday; vote terciles monotone overnight only). But the trade fails honestly: 2 trades/day ≈ 5–10%/yr drag, and post-2010 the overnight leg's DD advantage vanished (2022's bear was an overnight phenomenon: −23%). A diagnostic win, not a strategy. Offline (cached `overnight_panel.csv.gz`; `--refresh` refetches). |
| `run_liquidity_calendar.py` | QQQ + FRED components + VIX/UUP | Trades the liquidity *calendar*, not the level: tax dates, quarter-end RRP window-dressing, QRA, and post-debt-ceiling TGA rebuilds — all forward-dated. The plumbing verifies (tax +$87bn TGA in 97% of events; debt-ceiling +$223bn TGA / **−$121bn net-liq**, the only event that drains net liquidity). Halving the 2×vote in *debt-ceiling windows only* (mechanism-justified, 15% of days, n=10 events) is a strict champion upgrade: same 23% CAGR, Sharpe 0.83→0.86, DD −42%→−35%, Calmar 0.65, 96% CAGR-win vs SPY. The all-drains version trades 4pp CAGR for DD −30%. ⚠️ event list hardcoded through Jun-2023 — append new deals. Offline. |
| `run_exploration_lab.py` | QQQ/SPY OHLC + FRED + VIX/UUP | Four novel dials fired at the champion in one harness. **Winner: the tug-of-war layer** — soft-signed 21d (overnight − intraday) return spread, zero corr with all 4 existing layers, robust to window (10/21/42d) and underlying. Failures kept as findings: hysteresis (Schmitt trigger) blows DD to −49…−60% — the *continuous* mapping is the protection; vote velocity is noise (−1.2pp spread); liq-drawdown swap is softer but not better. The stack (5-layer +tug, ×½ debt-ceiling windows) is the project's best scorecard: CAGR 24%, Sharpe 0.94, **DD −31% < SPY's −34%**, Calmar 0.77, 2022 −28%, 99%/95% OOS win vs SPY. Fresh-sample check (data the selection never saw): the tug spread existed in 1994–2003 SPY (+36pp/yr) — not a recent artifact — but was dead 2004–2013 (−3pp): real, episodic, not yet promotable. Offline. |
| `run_tug_conditioner.py` | SPY/QQQ OHLC + VIX/IRX (1990→) | What conditions the tug regime? Six causal hypotheses double-sorted on SPY (vol regime, rate era, overnight variance share, spread persistence, trend state, trailing efficacy). Three look strong on SPY (persistence +19.5pp; low rates / low overnight-share ≈ −16pp) but **all six fail the QQQ confirmation** — every effect flips or collapses. Decision-relevant null: trailing efficacy doesn't predict forward efficacy (corr −0.05), so the episodicity is neither explainable nor timeable with this data. Treat tug's regime risk as undiversifiable → keep it diluted in the 5-layer vote, never standalone. Offline (cached `tug_conditioners.csv.gz`). |
| `run_retail_implementation.py` | SPY/QQQ + SSO/QLD (real prints) + FRED | The 5-layer vote at retail-accessible scale, GFC-inclusive (2008→). On **SPY the edge is modest** (Sharpe 0.58 vs 0.46 B&H, +3pp CAGR, same −50% DD) — the juice is a long-duration/QQQ phenomenon. Surprise: **the levered-ETF route wins**: weight = vote in QLD (or SSO) + cash beats the RF+50bp margin fiction by +1.3–1.6pp CAGR (QQQ/QLD: 24% CAGR, Sharpe 0.85, GFC −6%, 98% CAGR-win) — daily-reset compounding is convex in trends, financing embedded at institutional rates. Retail margin at 150bp costs only −0.3pp. Simplest implementation tested = best implementation tested (two tickers, weight = vote). ⚠️ daily-reset convexity reverses in choppy sideways regimes. Offline (cached `levered_etfs.csv.gz`). |
| `run_vehicle_switch.py` | QQQ/QLD + SPY/SSO (real prints) | Novel gap after a literature check (Gayed 2016 gates *exposure* by 200d MA; arXiv 2504.20116 says LETF compounding is autocorrelation-driven, but in theory only): hold the vote's exposure FIXED and switch the *wrapper* (QLD+cash vs retail margin) by regime. Primary metric = conditional wrapper spread. **Gayed's conditioner wins the head-to-head** (+2.4pp/yr separation, replicates on SPY/SSO); measured trailing autocorrelation fails (no separation, flips sign cross-asset); Hurst weak-positive. But none of it converts: "QLD always" (Sharpe 0.85) stays unbeaten because the LETF-hostile state is rare (19% of days) and mild (−0.6%/yr). Wrapper choice ≈ ±1–2pp/yr; the exposure dial dominates. Offline. |
| `run_rrp_floor.py` | FRED components + QQQ (2003→) | Structural-break forensics on the RRP's death (novelty certified — commentary only, no quant study found). **The absorber narrative fails**: even at $780bn, detrended RRP *rose* in tax windows (absorption −0.35) and net-liq pass-through was *more* than full (−1.76) — scarcity pushes money-fund cash *toward* the Fed, amplifying drains. Tax-window equity drag (−18pp/yr) was a pre-2014 scarce-reserve phenomenon, not an RRP story; liquidity-signal efficacy is higher unbuffered (corr 0.26 vs 0.16). Registers a dated forward prediction: with the buffer gone and QT ongoing, tax windows should start biting again — 2/2 post-buffer events fine so far; re-check after Sep-2026 + Jan-2027. Offline. |
| `run_auction_surprise.py` | TreasuryDirect API + TLT/QQQ (1,885 auctions, 2003→) | Trades the calendar's *outcomes*: per-auction demand surprise (bid-to-cover + indirect-share z vs trailing same-tenor norms, causal). **Mechanical check passes** — TLT prices the surprise same-day, monotone in both maturity buckets — **but equity transmission is a clean null**: QQQ day-0 flat, drift non-monotone (weak long-end auctions preceded the *best* 21d drift), aggregate 63d demand-health corr +0.01 with forward returns, and as a 6th vote layer it mildly hurts (Sharpe 0.79→0.77). The "auction tail = equity risk-off" commentary is a same-day bond story, not an equity signal. Offline (cached `treasury_auctions.csv.gz`). |
| `run_funding_stress.py` | FRED keyed API (SOFR/IORB/IOER/EFFR) + QQQ | The daily reserve-scarcity nowcast everyone watches and nobody backtests: (SOFR\|EFFR)−(IORB\|IOER), stressed = 5d MA ≥ 0bp (2008→, catches Sep-2019 +315bp). **The warning light doesn't warn equities**: stressed days → *better* fwd 21d returns (+25% vs +20% ann.), no fatter tail; liquidity-layer corr *lower* when stressed (0.12 vs 0.28); stress-throttling the champion mildly hurts (Sharpe 0.88→0.86). Mechanism: scarcity triggers Fed liquidity responses (2019 "not-QE") that are equity-positive — it predicts the *policy reaction*, not risk-off. Caveat: 2019 dominates the stressed sample. Kept as the live trigger/monitoring gauge for the RRP prediction, not as a vote layer. Offline (cached `funding_rates.csv.gz`; `--refresh` refetches, uses `FRED_API_KEY`). |
| `run_withheld_tax.py` | DTS FiscalData API + QQQ (2005→) | Zero-lag labor-income nowcast: daily withheld income/employment taxes (Table IV spliced with post-2023 Table II, ~10% definitional level break rescaled). As *macro* data it's superb — YoY −8.3% at the 2009 trough, −5.4% in 2020, +23.2% in the 2021 boom, ~2-day lag. As an *equity signal* it's a null: fwd-return corr +0.007, terciles non-monotone with the **high-growth tercile worst** (+12.9% vs +21.0% mid — faint good-news-is-bad-news), acceleration flat, and as a 6th layer it leaves the champion unchanged (Sharpe 0.79, 2022 −33% vs −35%). Labor income is real information that equities have already priced by the time it accrues. Offline (cached `withheld_taxes.csv.gz`). |
| `run_dealer_fragility.py` | NY Fed PD API + QQQ (2013→) | The "real" fragility gauge: primary-dealer net Treasury coupon inventories (weekly, four survey formats, within-segment causal rank, 8bd availability lag). The congestion story is visible in levels ($32bn → $301bn mean), but as a conditioner it **loses to its own VIX shadow**: forward-return terciles non-monotone, liquidity-layer conditioning non-monotone, and w_liq = 2×dealer-fragility underperforms both equal weights and the VIX version on the champion (Sharpe 0.88 vs 0.90, DD −41% vs −38%, CAGR-win 88% vs 97–98%). Slow-moving capacity ≠ tradable state: the price (VIX) embeds the plumbing better than the plumbing data does. Offline (cached `dealer_positions.csv.gz`). |
| `run_liquidity_shadow.py` | QQQ + FRED + stress panel (2007→) | The dealer-fragility autopsy turned on the thread's founding layer: can a causal price-only replica (rolling OLS of Δ6m net liquidity on IEF/UUP/SPY/credit/VIX features) span the FRED data? **Liquidity passes the test its four plumbing cousins failed.** Prices replicate the *level* well (OOS R² 0.44, corr 0.68) but not the *information*: the orthogonal residual keeps essentially all the forward signal (corr +0.25 vs raw +0.26, tercile spread +12.4%; shadow only +0.08) — and in the champion head-to-head the shadow-replaced vote collapses to the no-liq baseline (Sharpe 0.86 ≈ 0.85) while residual-only matches the real layer (0.91) with the better tail (DD −36%, Calmar 0.66, 2022 −33%). The one place plumbing genuinely leads prices — the FRED dependency earns its keep. Caveats: the edge is 2020→-concentrated (sub-period corr ~0 in 2010-2019) and the layer's total contribution stays modest (+0.06 Sharpe). Offline. |
| `run_riskoff_leg.py` | QQQ + 250-ETF universe + FRED (2010→; UUP check 2008→) | The champion's never-examined half: what should the strategy *hold* when the vote says retreat? Cash has been the default slack asset since the thread began. A 12-defensive conditional menu (duration, credit, TIPS, gold, commodities, dollar, staples/utilities, REITs) finds the apparent low-vote stars (XLP/XLU/VNQ +25-35% ann.) are conditioned equity beta (corr 0.67-0.80 with QQQ) — and the honest walk-forward selector chases exactly that and **fails** (Sharpe 0.89 < cash 0.93). But the one mechanism-true candidate wins: **UUP — the asset risk-off flows flow *into*** — is the only true diversifier with positive low-vote Sharpe (+12.3% ann., +0.81), and slack→UUP is a strict champion upgrade (DD −36% vs −38%, Calmar 0.68 vs 0.64, 2022 −32% vs −35%, 100%/90% OOS win vs SPY) that **survives the out-of-selection GFC check** (2008-2012: +96% vs +89%, GFC −3% vs −8%, stress-panel UUP the menu never saw). Risk-off is not rotation (selector fails) and not retreat (cash loses) — it's the dollar. Offline. |
| `run_liquidity_components.py` | QQQ + FRED components + stress panel (2007→) | Decomposes the founding layer: net liquidity = WALCL − TGA − RRP, but the three taps are different machines (policy / Treasury cash / money funds) — is the composite averaging away information? **No — the information is emergent in the netting.** Lead-lag profiles differ as predicted (WALCL short-horizon, −RRP long-horizon, −TGA null-to-negative) yet the composite beats every component at every horizon (Δ126 corr +0.23 vs +0.10/−0.05/+0.18; tercile spread +12.3% vs +5.3%/+1.8%/+5.5%), and in the champion every decomposed variant loses (component-mean 0.87, single components 0.84-0.87, evidence-weighted W/T/R 0.85 vs composite 0.91 Sharpe). Reserves = assets − TGA − RRP is not an arbitrary formula — the *net* is the economically real quantity, and no reweighting of the parts beats the identity. With the shadow test, the second independent attack the founding layer survives. Offline. |
| `run_dollar_slack.py` | QQQ/SPY + UUP + FRED (2008→, GFC-inclusive) | Promotion test for the risk-off-leg finding — three gates, all pass. (1) **Not redundant with the dollar layer**: the slack helps without the layer (4-layer DD −45%→−41%, GFC −8%→−3%) and the layer keeps its value with the slack — *signal vs asset*: the layer trades dollar momentum, the slack holds the dollar in de-risked states. (2) **New best scorecard on this harness**: 5-layer + ceiling ×½ + slack→UUP gives Sharpe 0.83, DD −41%, GFC −3%, **2022 −25%**, 99%/94% OOS win vs SPY — beats the prior best on every column, with monotone ½-UUP robustness. The interaction is mechanism-aligned: ceiling windows halve exposure, growing the dollar holding exactly when TGA rebuilds strengthen the dollar. (3) **SPY replication confirms direction** (DD −50%→−46%, GFC −21%→−17%, 2022 −20%→−17%). Promoted: the slack leg is UUP, not cash. Offline. |
| `run_retail_dollar_slack.py` | QLD/SSO/UUP real prints + FRED (2008→) | The dollar slack at retail scale: three tickers, one weight — w = vote in QLD, 1−w in UUP. Honest nuance vs the margin harness: the LETF route's slack is structurally large (≈1−vote daily), so always-on UUP drags ~1pp CAGR — **a tail upgrade, not a Sharpe-for-free upgrade**. Still, **QLD + UUP + ceiling ×½ is the new best retail scorecard**: Sharpe 0.86, Calmar 0.58, DD −40%, **2022 −24%** (vs cash-slack 0.85/0.55/−44%/−35%), 99%/97% OOS win vs SPY, with the per-year edge diversified across 2008/2014-16/2018/2022/2024/2026 — and ½UUP keeps the full 0.85 Sharpe with most of the tail gain. SPY/SSO replication is mixed at this scale (DD better, Sharpe slightly worse) — the juice remains a QQQ/long-duration phenomenon. Offline. |
| `run_disagreement_fresh.py` | SPY 1993→ + QQQ 1999→ OHLC | Fresh-sample verdict on the unvalidated inverted disagreement throttle: 4 price-only stances (trend, meanrev, BOCPD, walk-forward HMM) rebuilt over decades the selection never saw. **The throttle dies; the diagnostic survives.** The return leg (high model dispersion = good times) fails out of sample — SPY 1994-2003 inverts back (+6.6% low-D vs −0.1% high-D fwd 6m), 2004-2012 is monotone the wrong way, and even the 2013→ bridge goes flat once the liquidity stance is dropped — so the inverted throttle degrades Sharpe in *every* window vs a plain trend dial (its 2022 magic was a 5-stance, one-sample artifact; formally killed). But the transition tell — **indecision precedes 200d-MA flips** — validates strongly: flip probability within 21d is monotone-decreasing in dispersion in 4/5 windows (SPY 2004-12: 32/28/14%; SPY 2013-26: 28/17/10%; QQQ 2000-12: 44/23/18% vs ~25-30% unconditional) across 30 years and two assets. Model agreement isn't safety — it's the signature of a regime about to flip. Keep D as a regime-age diagnostic; never as an exposure dial. Offline. |
| `run_vote_cross_section.py` | 250-ETF universe + FRED (2012→) | First cross-sectional use of the vote: does the timing edge concentrate in assets that co-move with the dial (vote-beta = trailing 3y corr of asset returns with the vote level)? **No — co-movement is the wrong statistic.** The edge is broad with an inverted-U (fwd-21d timing edge +8.5/+11.7/+10.6/+8.7% across Q2-Q5, only the bond-like Q1 lacks it), the max vote-beta in 250 ETFs is a tiny ~0.08 (the top decile is munis/dividend/preferred — rate-sensitivity, not risk appetite), and QQQ itself sits at the **43rd percentile** of vote-beta. The causal top-10 selection collapses (2×vote on the basket: Sharpe 0.38 vs 0.88 on QQQ). Verdict closing the underlying question for good: dilution fails (stress test), selection fails (here) — **the dial doesn't pick the asset; the asset's unconditional growth beta carries the dial**, and the vote's job is risk-shaping on a high-return underlying, not asset-picking. ⚠️ survivorship-biased universe (judge spreads, not levels). Offline. |
| `run_flip_risk_modulator.py` | QQQ + stress panel + FRED (2011→) | The right use of the validated flip-tell: a flip probability is direction-agnostic ("this state won't last" ≠ "returns will be bad"), so it should modulate *how* the champion trades, not *whether* it's invested. Sanity passes on this sample (flip prob 26.7% high-f vs 17.6% uncond). **Neutral-shrink fails** (e′ = 1+(e−1)(1−f): Sharpe 0.87 < 0.92 — even shrinking to 1× gives up too much upside) and the dead cash-throttle reference stays dead (34% CAGRwin). **Slow-down passes**: e′ = 2×[f·MA63(vote) + (1−f)·vote] — trade the smoothed vote when a flip is likely, the crisp vote when the regime is stable — preserves the full scorecard (CAGR 0.24, Calmar 0.64, Sharpe 0.90 vs 0.92, 2022 −34%) with **41% less turnover** (9.8× vs 16.6×/yr) and the thread's best OOS consistency: **SRwin 93% vs 85%**, CAGRwin 99%. The probability signal monetizes as patience, not position. Marginal refinement in costs at 2bp TC; the gap widens at realistic retail frictions. Offline. |
| `run_champion_stack.py` | QQQ/QLD/UUP + FRED (2011→) | **The frozen reference configuration** — joint test of everything this session validated, since interactions could break pieces that passed separately. The additive ladder holds: champion (Sharpe 0.92, DD −38%) → +ceiling ×½ (0.94, −31%) → +UUP slack (0.94, −29%, 2022 −25%) → +flip-risk slow-down (0.91, −29%, turnover 23.9×→10.4×, SRwin 99%); leave-one-out confirms each piece earns its place jointly. The headline: **the full stack in its retail wrapper (w = e/2 in QLD, 1−w in UUP — two tickers, one weight) is the best scorecard in the project: Sharpe 0.96, DD −28%, Calmar 0.87, 2022 −24%, 100%/97% OOS win vs SPY, 7.5× turnover/yr.** Spec frozen in the docstring (vote layers, slow-down, ceiling list ⚠️ append new deals, UUP slack, costs) — nothing re-fitted in this study. Offline. |
| `run_adaptive_vote.py` | QQQ + stress panel (2007→) + FRED | Questions the winner, GFC-inclusive. (1) **GFC pass**: 5-layer equal vote live 2008-04, −8% through Lehman vs QQQ −21%/SPY −27% — tug doesn't hurt in its dead decade. (2) **Bayesian layer weights** (w = 1 + positive trailing-corr evidence, parameter-free) ≈ equal weights — a 1/N result: Sharpe 0.91 vs 0.93, but best tail profile when stacked (Calmar 0.82, DD −30%, 2022 −26%). (3) **Ceiling autopsy downgrades the calendar claim**: the ×½ overlay helped in only 4/10 episodes; the edge is dominated by Dec-2021 (+9.8pp, the window that happened to contain the start of the 2022 crash). A concentrated drawdown clipper with n_eff ≈ 2, not a broad mechanism. Offline. |

| `build_fred_macro.py` | 20 FRED series (2010→) | **Builds the macro panel** — rates & curve (DFF, DGS2/10/30, DFII10 real yield, T10Y2Y/3M), credit (BAA10Y/AAA10Y full-history; ICE BofA OAS ⚠️ API-capped to ~3y), VIX, NFCI/STLFSI4, breakevens, dollar (broad + EUR), WTI, jobless claims → `datasets/fred_macro.csv`, each series shifted by its publication lag so row t is information available at t. `--with-net-liquidity` also refreshes `net_liquidity.csv`. Network; needs `FRED_API_KEY` (free) in `.env`. |
| `run_timing_mining.py` | QQQ (or any ETF in the universe) + FRED panel | **Timing-dial mining** — the miner pointed at the question that actually pays on this data: evolve exposure dials e ∈ [0,1] on one underlying from macro z-scores (any panel series, learned sign/steepness), price trend/momentum, realized vol, and BOCPD regime age. Same randomized gauntlet, but the null strategy is buy & hold, not cash: VALIDATED requires beating B&H's holdout Sharpe with bootstrap p < 0.10, permutation p < 0.10, DSR > 0.50. `--seeds N` adds the family-recurrence table; two vote families (`macro_vote2`, `macro_trend_vote`) search the champion's layered architecture. 2 bps costs, 1-day lag. The first 5-seed QQQ run taught the system its hardest lesson: **easing dials recurred in 5/5 seeds and three DGS2 dials formally VALIDATED** (holdout Sharpe ≈0.94 vs B&H 0.77) — then the fresh-sample check on 1999–2009 killed them (−0.02 vs 0.15: pre-QE the Fed eased *into* recessions). That check is now built in as an automatic final gate: each finalist is re-run on pre-2010 history (yfinance, cached as `datasets/fresh_<ticker>.csv`; panel extends to 1990) and VALIDATED upgrades to **PROMOTED** only by beating B&H there, else **ARTIFACT** (**VALIDATED\*** when the dial's inputs don't exist pre-sample). `--no-fresh` to skip. Offline after first run. **Definitive 5-seed × {QQQ, SPY} verdict**: macro families own the recurrence tables on both underlyings; every *nominal*-easing genome that VALIDATED (DGS2/DGS10/DGS30, NFCI+DGS30 votes) came back ARTIFACT on the fair-window fresh gate — except the **real-yield dial (DFII10, risk-on when 10y TIPS yield sits below its rolling norm), PROMOTED on both underlyings** (fresh 2003–2009: QQQ 0.93 vs B&H 0.49; SPY trend-vote 0.65 vs 0.36) and consistently fresh-positive across seeds. Mechanism is distinct from nominal easing: real yields *rose* in 2008, so the dial de-risked into the crash. ⚠️ one fresh cycle (DFII10 starts 2003), and the SPY-only NFCI-vote promotions fail QQQ confirmation — treat only the real-yield dial as a promotion candidate. |
| `run_council_backtest.py` | QQQ (any ETF) + FRED panel + net liquidity | **Blinded council backtest** — the backtest decomposed into period-isolated decisions by a council of domain specialists (rates, credit, vol, dollar, liquidity, trend). Each month-end every specialist sees only a BLINDED slice of trailing data (no dates, tickers, or levels — z-scores and normalized paths, randomized presentation) and votes a stance; the confidence-weighted vote becomes next month's exposure. Designed for LLM analysts without lookahead-through-memory: `--llm` runs Claude-backed specialists (structured JSON stance/confidence via the API, Batches API at 50% cost for full runs, `ANTHROPIC_API_KEY` in `.env`), and the **mirror audit** measures residual leakage — every specialist also rates a sign-flipped copy of each context; an honest analyst's stances must invert (anti-symmetry ≈ 1) and sum to zero (leak_ratio ≈ 0) — an LLM failing either is recognizing episodes, not analyzing. First offline run (statistical council, QQQ 2010→): council Sharpe 0.98 vs B&H 0.95 at **less than half the drawdown** (−16% vs −35%); liquidity and credit are the strongest solo seats. Offline by default. |
| `run_council_oracle_test.py` | QQQ (any ETF) + FRED panel | **Falsifies the council's lookahead detector before any LLM is trusted with it.** OracleSpecialists simulate the exact failure mode — a stance partly keyed on episode *identity* (perfect for mirrored contexts, like an LLM recalling "March 2020 recovered") — at controlled leak doses. Three checks: (1) **dose-response** — Sharpe inflates 0.98→1.48→2.5 as leak rises 0→0.25→1; the first audit statistic (anti-symmetry) **missed the 25% dose** (0.83 > the 0.8 flag bar) while Sharpe was already +0.5 inflated, which motivated the second statistic: **leak_ratio** = std(stance+mirrored)/(std+std), identically 0 for honest analysts, 0.29 at a 25% leak → now every nonzero dose is flagged. (2) **localization** — corrupt one seat, exactly that seat flags (leak_ratio 0.94, five honest seats at 0.000). (3) **repair** — dropping flagged seats returns the council to the honest baseline (corrupted 1.31 → repaired 1.01 vs honest 0.98). The operational protocol for a future LLM council: audit → drop suspects → trust the remainder. Offline. |
| `run_council_multiasset.py` | SPY/QQQ + 9 SPDR sectors + TLT/GLD/UUP, Alpaca costs | **Multi-asset council at retail scale.** The 6-seat macro council sets a monthly risk dial; the risky sleeve spreads across the basket (inverse-vol, bounded ±50% relative-strength tilt, fit-free); slack goes to cash / TLT+GLD / UUP. Alpaca retail costs (~1 bp one-way ETF half-spread + 0.3 bp sell fee) on actual turnover — long-only, no margin, replicable as-is (~5 bps/yr realized drag). **The ablations are the finding**: the sector tilt adds nothing (Sharpe 0.80 with = without; third independent confirmation that the dial doesn't pick the asset) and the 11-ETF basket *dilutes* (0.80 < SPY-only dial 0.85). What works is the validated recipe at retail simplicity: **council dial on QQQ + UUP slack — two tickers — Sharpe 1.05, DD −16% vs SPY's −34%, Calmar 0.70 vs 0.42**, +12.5pp in both SPY-down years (2018, 2022), mirror audit clean. Honest caveat: CAGR 11.1% trails SPY's 14.1% (the e∈[0,1] dial trades upside for tail protection; the thread's levered variants are the CAGR route). Offline. |
| `run_quant_pod.py` | QQQ (any ETF) + FRED panel | **Walk-forward quant pod**: the full pod loop — each quarterly review the timing miner runs on data up to that date only; candidates beating buy & hold on the slice's recent 18-month holdout (bootstrap + permutation gates) are HIRED into one of 3 book seats with frozen parameters; sleeves are RETIRED by a monthly risk stop (live DD > 1.5× research DD), review-time decay detection (trailing-1y live Sharpe < 0), or two consecutive re-validation failures; idle seats sit in cash. Alpaca ETF costs. Every decision is walk-forward; the honest headline is the **IS → LIVE haircut table** (research Sharpe at hire vs realized Sharpe while deployed — the direct measurement of whether the alpha was real and when it was harvested), plus the hire/fire journal. **First full run (QQQ, 2017–2026, 22 hires / 19 fires): the machinery works — the risk stop fired three sleeves into March 2020, decay caught the dead EUR dial, the 2022 oil vote was cut at −1.76 live — but the book (Sharpe 0.87, DD −24%) does NOT beat dumb half-exposure QQQ (0.94, −19%), and the haircut table shows why: corr(research Sharpe, live Sharpe) = −0.52 — hires claiming >1.5 delivered 0.29 live; hires claiming ≤1.0 delivered 1.72.** Per-review re-selection is a selection machine: the harder a candidate won the gate, the more its win was luck. The fixed champion-stack/council approach beats the adaptive pod on this data. Offline, ~30 min. |
| `run_hiring_policies.py` | QQQ (any ETF) + FRED panel | **Pod hiring policies — can the winner's curse be traded against?** Same pod, same gate, same retirement rules; the only change is the ORDER gate-passers fill free seats: `raw` (highest holdout Sharpe — the status quo, bitwise identical to `run_quant_pod.py`), `dsr` (highest Deflated Sharpe Ratio), `shrunk` (order by predicted live Sharpe from a live-vs-research regression on the pod's own completed sleeves — the negative slope is learned, not assumed), `median` (fill seats from the median-ranked passer outward; the top pick is hired last), `inverse` (lowest research Sharpe first — the pure exploit of the negative slope, with hindsight of its sign), `veto` (shrunk + refuse any passer with predicted live Sharpe ≤ 0; can act even at single-passer reviews). Research is cached per review (deterministic miner) so six policies cost one run. **Result (QQQ 2017–2026): the curse is real — live = 2.07 − 0.73 × research (n=21, p=0.016); top-third research Sharpe delivered 0.58 live vs 1.72 for the bottom third — but it is a guardrail, not alpha: every anti-curse policy beats raw only directionally (median 0.98/−16.5% DD, P(>raw)=0.70; inverse 0.96, veto 0.89) and NONE clears dumb half-QQQ (0.94) — median P=0.58, and even `inverse`, the maximal-hindsight exploit of the slope, is a coin flip at P=0.51.** raw == dsr bitwise (DSR is monotone in holdout Sharpe within a review — it gates levels, it cannot reorder a review's survivors); `shrunk` shares 17/22 hires with raw (reordering is structurally throttled at 3 seats with few multi-passer reviews). **Out-of-sample epilogue: `median`'s QQQ win FAILED replication — on SPY it is the worst policy (0.58 vs raw 0.75, P(>raw)=0.09), flipping sign across configurations; the `sized` capital overlay (seats weighted by predicted live Sharpe) is harmful in all four configs; and widening the search space with short-horizon/lag/combination families (`--extended`: macro_lag_dial, rev_dial, lag_vote2) made raw WORSE on both underlyings (QQQ 0.87→0.79, SPY 0.75→0.68) — more combinations and lags = more trials = the gate passes more luck. Every configuration (7 policies × 2 underlyings × 2 search spaces) lost to half-exposure buy-and-hold.** The robust lesson is to avoid the top of the gate-passers list (top-third research Sharpe delivered 0.35–0.58 live vs 1.46–1.72 for the bottom third, on both underlyings), not that any re-selection policy is alpha; Epilogue 2 (`--gate strict`, `--with-liquidity`, `--rich-macro`, `--underlying QLD`): tightening the gate is the WORST lever (QQQ 0.63, SPY 0.34 — stronger in-sample evidence selects harder winners, exactly what the curse punishes); richer FRED inputs (net-liquidity complex + 7-series fred_macro_plus) flip sign by underlying and clear no benchmark; on QLD (2×) the pod beats its own half-QLD benchmark for the first time (raw 0.89–0.93 vs 0.86, P ≤ 0.70) but still loses to plain QQQ B&H over the same span (0.94 Sharpe, 19.4% CAGR, −35% DD). Epilogue 4 (`run_levered_barbell.py`): the improvement that finally moved the frontier is neither mining nor selection — a static ~50/50 QLD/GLD barbell (21.7% CAGR, Sharpe 0.99, −39.5% DD) beats QQQ B&H, and the blinded council dial on top is the one overlay that decisively earns its complexity, on drawdown control: 23.0% CAGR, Sharpe 1.04, −33.5% DD, Calmar 0.68, P(beat DD-matched static mix) = 0.91. Caveats: gold's decade was exceptional; GLD was a post-hoc pick (UUP, the pre-validated slack, gains nothing over its static mix). Epilogue 5 (`run_barbell_robustness.py`): de-hindsighting kills the discovery claim — walk-forward selection of the slack leg lands below QQQ B&H under both disclosed selectors (trailing-Sharpe degenerates into T-bills; the vol-floored version picks gold 56/60 quarters yet loses ~5pp/yr to fixed GLD via TLT/commodity detours), and the SSO/SPY leg fails outright. The barbell survives only as a strategic fixed conviction (cf. WisdomTree GDE), with the council dial as its drawdown governor. Epilogue 6 (`run_barbell_governors.py`): four novel governors on the strategic barbell, one a-priori parameterization each — vol management is borderline (P=0.89 DD-matched, Calmar 0.59 vs 0.55, but loses wealth to its own constant average scale), while the correlation-spike governor, VIX-slope scale and council-disagreement de-risking are all dead; structurally, overlays should ROTATE between the legs (council-as-rotation P=0.91) rather than scale to cash (same dial as cash-scaler P=0.85) — cash governors give up the second risk premium. see `docs/pod/HIRING_POLICIES.md`. Offline, ~5 min/run (needs FRED_API_KEY only to rebuild the plus-panel). |
| `run_barbell_stack.py` | QLD/SSO + GLD + council minutes | **The stack test**: the thread's three surviving pieces (strategic levered-equity/gold barbell, council dial as leg rotation, volatility management) run JOINTLY for the first time, with leave-one-out bootstrap. **Verdict: the survivors are substitutes, not complements** — rotation alone beats the stack on wealth in 99%/96% of draws (QLD/SSO), the stack's Calmar gain over the best single overlay is ≤ 0.01, and the mechanism is overlapping de-risk triggers (corr 0.43 between the council exposure and the volmgmt scale; jointly de-risked 35% of days vs 25% independent). Pick ONE governor per barbell: council rotation for QLD/GLD, volmgmt for SSO/GLD. Governor stacking joins the dead list. Offline. |
| `run_nowcast_descent.py` | 13-ETF basket (SPY/QQQ + sectors + TLT/GLD) | **Gradient descent on weights vs a nowcast, from all standpoints.** Foundation first: measured EWMA nowcast R² on this data — vol 0.246 vs returns 0.032 (8×; TLT/GLD return R² ≈ 0), so the two objectives were never symmetric. Min-vol descent works (delivers the realized vol it optimizes, 8.1%; Sharpe 1.09–1.16, best in table; 2022 −9% vs SPY −18%) but is directional not decisive vs inverse-vol (P ≤ 0.83) and is a defensive allocation (CAGR 8.8% vs EW 12.5%). **Max-nowcast-return fails exactly as theory predicts** (P = 0.05 vs inverse-vol — the error-maximizer / winner's curse at weight frequency). What gradient descent itself buys is turnover, not Sharpe: OGD = 93% of the exact QP's Sharpe at 1/5 the turnover (implicit shrinkage to yesterday's weights). Do not stack on the barbell governors (third correlated vol channel, cf. Epilogue 7). Offline. |
| `run_return_tilt.py` | QLD + cash (+ rotation context) | **Tilting toward maximum returns, honestly**: with returns unpredictable, return-max = premium exposure sized by risk — constant-premium Kelly (μ̄ = 7% fixed, never re-estimated) and vol targeting on the working vol nowcast, cap 2×, retail QLD+cash, Alpaca costs. Verdict: **nothing beats holding the max leverage on wealth** (QLD B&H 69× at −64% DD; best challenger P = 0.13) — under a cap, sizing can only hold less, and bull years dominate this sample. The dose-response is exact (σ* 15/20/25% ⇒ CAGR 13.5/17.6/21.1%): the return dial IS the drawdown budget. Efficient menu: −64% → QLD B&H 69×; −39% → full Kelly 30×; −34% → rotation barbell 24× (best Calmar, dominates QQQ B&H); −23% → voltarget 15%. ⚠️ no GFC in sample: 2×-always through 2008 was a −85% event — the survival argument for sizing is exactly what a 2011-start backtest understates. Offline. |
| `run_barbell_gfc.py` | QLD real prints 2006→ + stitched GLD/QQQ + FRED from 2005 | **The GFC test of the final configuration** — zero re-fitting: the fit-free council run on stitched 1999→2026 QQQ (mirror audit CLEAN), frozen barbell weights, real QLD prints through 2007–2009. **The rotation barbell passes its first systemic crisis**: GFC −41% (QQQ −51%, QLD −82%), 2008 −31%, 2009 +101%, 20-year Calmar 0.48 at 65× wealth — best in table. Corrects Epilogue 9's frontier with the crisis sample: max-leverage B&H's 106× required living through −83%. Rotation = wealth-efficient governor, volmgmt = DD-efficient (−41% GFC, 0.41 DD, 29×) — the Epilogue 7 division of labor replicates out-of-regime. n = 1 crisis, disclosed. Network on first run (pre-2010 GLD; FRED net-liquidity rebuild from 2005), cached after. |
| `run_alpha_mining.py` | 500 equities (or 250 ETFs / ~100 S&P) + FRED panel | **Automated alpha mining** — the research loop, automated: an evolutionary search (selection/crossover/mutation/immigrants) over 9 parameterized signal families (momentum, short-term & idiosyncratic reversal, vol-scaled momentum, low-vol, channel breakout, MA cross, skewness, BOCPD regime-gated momentum, and — when `datasets/fred_macro.csv` exists — credit-gated momentum and VIX-gated reversal) × portfolio construction (rank vs quantile, smoothing). Fitness is scored on freshly *randomized* purged 1-year windows redrawn every generation (mean Sharpe − dispersion penalty), so no candidate can overfit a fixed split; finalists then face a gauntlet on an untouched 30% holdout — stationary block bootstrap, circular permutation test, and a Deflated Sharpe Ratio charged for every unique candidate the search examined. Dollar-neutral L/S, gross = 1, 1-day lag, 10 bps turnover cost. Most candidates come out REJECTED — by design; the system's value is saying so honestly. ⚠️ equity universe is survivorship-biased. Offline. |
| `run_factor_attribution.py` | champion returns + FF daily | **The Buffett's-Alpha regression of the champion stack** (needs `run_champion_stack.py` once, which now persists `results/champion_stack_returns.csv`): daily excess returns on FF5+UMD, Newey-West. Verdict: the stack is α = +6.1-6.9%/yr at t(α) ≈ 2.3-2.6 with average market beta ~1.0 (the 2× cap is rarely fully spent) and an anti-value tilt (HML −0.36); the QQQ B&H control gets +2.6% (t 2.07) from the same model, so the timing machinery adds ~+3.5-4.3pp/yr beyond static growth beta — survives the classical bar, fails Harvey's t>3. Offline. |
| `run_alpha_halflife.py` | 212 OpenAP predictors, 1926→2024 | **The alpha half-life trade — factor-zoo age structure as a signal** (McLean-Pontiff as an allocation rule, not a discount). The decay fact replicates (50% post-publication haircut), but the event-time profile is a STEP at publication, then flat for 25 years — so age carries no signal once published, and the tradable YOUNG−OLD spread is dead (−0.7%/yr, t −0.3). Byproduct worth keeping: EW of all published factors = Sharpe 1.21 (t 5.9, maxDD −6%, gross), stable across sub-periods — diversification across the zoo, not its age structure, is where the value is. YOUNG leg starves after 2019 (publication rate collapsed post-2016). Clean null on the headline idea. Offline. |
| `run_factor_momentum.py` | 212 OpenAP predictors, live 1997→2024 | **Factor momentum on the zoo vs the EW-ALL benchmark** (Ehsani-Linnainmaa, roadmap Idea 3 on the OpenAP panel). The fact replicates: XS winners (top ⅓ by trailing 12m) Sharpe 0.83 (t 4.4) vs losers ≈ 0, TS winners Sharpe 1.01 (t 5.4), the 12m XS spread +9.5%/yr (t 2.6) — all sub-period stable. But against the Idea-7 benchmark it **adds nothing**: the spread's return is ~1.9× beta to EW ALL with alpha +0.9%/yr (t 0.25), and the pre-declared 50/50 vol-matched combo LOWERS the Sharpe (1.09 vs EW ALL's 1.29, t 6.8, maxDD −6%). Factor momentum here is mostly time-varying exposure to the zoo itself, not a new return stream — and that's gross; the spread's 2× leverage and sleeve turnover make net even worse. Hold the zoo, don't time it. Offline. |
| `run_retroactive_dsr.py` | 5-ETF panel + champion returns | **Retroactive DSR on the hand-built families** (roadmap Idea 4 — the multiple-testing tax the miner pays but the flagships never did; needs `run_champion_stack.py` once). Split verdict. **Family A (5-ETF v1→v4 arc, 2016-2024): FAILS** — best variant `bocpd_amr_v4` DSR 0.91 at its nominal 9 trials (excess-return Sharpe 0.58), sliding to 0.88 at an honest 80; v3 = v4 to three decimals, and v2 *underperforms* v1 — the version arc was noise, not progress. **Family B (champion-stack ladder on QQQ, 2011→): PASSES** — every variant DSR ≈ 1.00, robust to 80 assumed trials AND to swapping in family A's 14× larger trial dispersion (0.998); 14.5y of daily excess Sharpe ~0.92-0.96 is far above any plausible expected-max benchmark. The program's center of gravity is where it already moved: the QQQ vote stack is statistically real, the 5-ETF allocation engine never cleared the bar its own miner enforces. Offline. |
| `run_factor_crowding.py` | 212 OpenAP predictors, live 1997→2024 | **Factor crowding on the zoo — can the correlation structure warn the EW holder?** (completes the OpenAP triptych: age null, momentum null, now crowding). Three causal 36m gauges (avg pairwise corr, PC1 share, 12m return dispersion), expanding-rank terciles + rank IC vs EW-ALL forward returns. Verdict: **no crowding trend and no usable warning gauge.** The zoo DE-correlated as it grew (avg corr 0.05→0.03, PC1 share 0.44→0.28 since the 90s) — published-factor capital did not tighten the structure. The two correlation gauges disagree on sign within eras (avg_corr weakly negative, PC1 weakly positive, neither t>2) — noise. The one borderline live wire is *dispersion*: high cross-factor 12m dispersion → better EW month ahead (IC +0.14, t 2.7 full / t 2.2 post-2004) — an opportunity-set effect, the opposite of a crowding alarm; one-era, 6-trial ledger, not promotable. Standing caveat: gross academic panel can't see cost/capacity crowding — this null does NOT clear live crowding risk. Offline. |
| `run_zoo_metafactors.py` | 212 OpenAP predictors, live 1997→2024 | **Meta-factors and dynamic clusters on the zoo — the first non-null on this panel.** Three factor-level cross-sections beyond momentum + K=6 monthly hierarchical clustering (causal, 36m corr, median month-to-month adjusted Rand 0.75 — clusters are real and nowcastable; they map loosely onto Cat.Economic). Dead: LT reversal (t(α) 0.5), factor-level low-vol (backwards, −4.2%/yr — high-vol factors earn more), cluster rotation (t(α) 1.2). Decaying: cluster-balanced 1/K weighting (α t 4.9 pre-2004 → t 2.0 after; post-2004 Sharpe 1.06 *loses* to EW's 1.40 — not promotable). **Survivor: factor-level seasonality** (Keloharju — long factors strong in this calendar month, 10y same-month mean): +9.0%/yr, t 3.6, **α vs EW +9.5%/yr (t 3.6) at β −0.12**, not momentum in disguise (corr −0.20, two-factor α t 3.7), alive in both eras (α t 2.0 / t 3.4), and the 50/50 vol-matched combo improves the EW holder in BOTH eras (Sharpe 1.42 vs 1.29, maxDD −4% vs −6%). Clears Harvey's t>3 with literature priors behind it. Caveat: gross 2×-levered monthly-turnover academic construct — a research fact, not yet a tradable claim. See the OOS test below before quoting the magnitude. Offline. |
| `run_seasonality_oos.py` | JKP 13 themes × 4 regions | **The cross-region kill test of factor seasonality** — same recipe, zero re-tuning, on the panel the discovery never saw. Verdict: **direction survives, magnitude does not.** All three true-OOS regions are positive and half-sample stable (developed/emerging/world_ex_us: Sharpe 0.37-0.42, t 2.1-2.5, α vs regional EW t 1.2-2.0) — but at ~1/6 the OpenAP magnitude, and the US *theme-level* panel shows nothing (t 0.76 over 69 years, second half negative) despite being the discovery country. Read: factor seasonality is a BREADTH effect — it lives in fine-grained factor cross-sections (~60 per leg on OpenAP) and dies when 153 factors are folded into 13 themes (4 per leg). Discount the t-3.6 headline accordingly; the Idea-10 combo improvement is real on the panel it was found on, unconfirmed elsewhere at equal breadth. Offline. |
| `run_seasonality_matched_oos.py` | JKP 153 individual factors × 4 regions | **The decisive OOS at MATCHED breadth** — the test the theme-level run couldn't do. Now ~150 factors per region (not 13 themes), so the breadth excuse is gone and t resolves. Verdict: **the signal is real out of sample, but the OpenAP magnitude was an artifact, and costs cap it.** The three geographic-OOS regions all confirm cleanly — developed α +3.4%/yr (t 3.9), world_ex_us +3.4% (t 4.3, β 0.02 & corr_mom −0.01 → a nearly *pure* orthogonal stream), emerging +2.3% (t 3.1) — orthogonal to momentum and stable in both eras (mostly t>2.7 each half). But two downgrades: (1) magnitude is ~3%/yr gross, a third of the OpenAP +9.5% headline; (2) **usa on JKP does NOT replicate** — t(α) 1.78, β 1.18, dead post-2004 (t 0.61), so the OpenAP US result was panel-specific. Cost grid is the promotion gate: the sleeve turns over ~12×/yr, break-even ≈25-30 bps per unit one-way turnover (net Sharpe ~0.4-0.5 at 10 bps, ~0 at 30 bps) — survives institutional frictions, dies at retail. Net: downgraded from "the headline positive" to "real, modest, momentum-orthogonal, cost-fragile"; promising only as a low-turnover-engineered or market-neutral overlay sleeve, never a retail standalone. Offline. |
| `run_seasonality_lowturn.py` | JKP 153 individual factors × 3 regions | **Can seasonality be traded? Low-turnover constructions** — the promotability follow-up. Three builds of the same signal: hard monthly tercile (baseline), continuous z-score weights, and hysteresis-buffered tercile (enter at top/bottom 1/3, exit only past the median). **Buffering is strictly better than the baseline** — it *raises* gross Sharpe (developed 0.65→0.73, world_ex_us 0.70→0.78) AND cuts turnover ~27% by not trading boundary flicker — but the cost break-even only moves from ~15 to ~18 bps. Continuous cuts turnover most (~45%) but dilutes the signal (gross ~1.8%/yr). **All three die by 20-30 bps**: turnover is largely STRUCTURAL (the signal reconstitutes every calendar month), not reducible. Final word on seasonality: an institutional market-neutral overlay sleeve, best built buffered, break-even ~18 bps — not retail-viable at any construction. Offline. |
| `run_factor_regimes.py` | 212 OpenAP predictors, live 1997→2024 | **BOCPD on every factor — regime age as a meta-signal** (novel: the program's signature changepoint tool lifted to the factor-of-factors level; no published precedent in the KB). Per-factor online Bayesian changepoint posterior (hazard 1/36), three pre-declared questions. Verdict: **the Daniel-Moskowitz mechanism is real at the factor level, but it's not tradable here.** Every comparison goes the predicted way in both eras — stale-regime factors beat fresh (α t 2.24 vs −0.36), momentum conditioned on stale regimes beats unconditional beats fresh (Sharpe 1.06 / 1.01 / 0.82), and the dose-response inside the momentum book is near-monotone in ERL quintile (0.66 → 1.03) — yet the regime-age spread is only t(α) 1.5, the conditioned momentum still has no alpha vs EW ALL (t 1.25), and the systemic changepoint dial is dead (IC sign flips across eras). 8 trials. A mechanism confirmation that adds another brick to the wall: even the program's own regime machinery can't time the zoo past its diversification. Offline. |
| `run_zoo_absolute.py` | 212 OpenAP predictors, live 1997→2024 | **The absolute-return zoo: value/momentum 50/50 and the levered stack** (user prompt — chase total return, beta/alpha indifferent). The AQR premise **fails at the factor-of-factors level**: MOM/VAL correlation is +0.21 full sample and ~0 post-2004 (no negative-correlation magic) because factor-level value is just too weak (LT-reversal sleeve Sharpe 0.13), so VM 50/50 (Sharpe 0.47) *underperforms* momentum alone (0.50). Adding seasonality helps (VMS inv-vol 0.76, MOM/SEAS corr −0.20), but folding sleeves into the EW carry *dilutes* it (ALL-IN 1.21 < EW 1.29). The absolute-return answer is the simplest one: **lever the EW zoo itself** — causal 10%-vol target (cap 3×) turns +4.6%/yr into **+10.9%/yr gross, Sharpe 1.37, maxDD −15%, era-stable (1.27/1.44)** and beats the equally-levered all-in stack on every line. Even with alpha off the table, nothing on this panel improves on holding everything; leverage, not selection, converts the zoo's quality into quantity. Gross ceiling: monthly turnover, 2× sleeves, hard-to-short names — costs come off before any of this is real. Offline. |
| `run_zoo_tracking.py` | OpenAP target + FF12 / 27 ETFs | **Can a long-only, no-leverage portfolio track the levered zoo?** (user prompt). Min-TE replication of the Idea-12 winner (EW zoo @10% vol, 3× cap) under w≥0, Σw≤1 + cash, two universes (FF12 industries 1998→, 27 curated ETFs 2010→), in-sample floor + 60m walk-forward. Verdict: **the stream is unspanned by long-only space — a low-TE replica does not exist.** The optimizer's best answer is ~90-97% cash (in-sample TE floor 7.1-8.1% vs the target's own 8.0% vol — ≤20% of variance reachable even with hindsight; walk-forward corr 0.11 at best). Worse for the premise: equities are *negatively* correlated with the stream (SPY corr −0.42, TE 18.5%), so adding beta moves you *away* from it. The economic content: the levered zoo IS the orthogonal complement of a long-only book — it can be added (via market-neutral vehicles) but not rebuilt from beta. SPY did match the target's absolute return over 2010-24 (+14.4% vs +10.6%) at 2× the vol and −34% maxDD — same destination, entirely different (and worse) road. Offline. |
| `run_reachable_frontier.py` | 20 liquid ETFs (2011→) + zoo target | **The reachable frontier — what you can actually buy, NET of costs.** Closes the arc: if the zoo is unreachable AND a diversifier, is the program's "diversify then vol-target" lesson worth holding long-only? Seven books (SPY, 60/40, flat inverse-vol, group risk parity, defensive tilt, two vol-capped), grouped so no sleeve dominates, causal, 5bp turnover, no leverage. **Honest negative: over the 2011-2026 regime nothing beat SPY risk-adjusted** — SPY Sharpe 0.90 (realized RF, fair to low-vol) and 60/40 0.89 vs the diversified books' 0.62-0.72. Diversification bought drawdown insurance, not Sharpe: maxDD −12-14% vs SPY's −24%, and 2022 −8% vs −18%. And every reachable book is *negatively* correlated with the levered zoo (ρ −0.31 to −0.41) — confirming run_zoo_tracking from the other side: none of them harvest the zoo, they're all just beta in different doses. The synthesis of the whole sub-arc: the absolute-return king is a genuinely orthogonal market-neutral stream reachable only through actual market-neutral vehicles, not clever long-only weighting; in reachable space over this bull-dominated regime, concentration (SPY) won and diversification was an insurance premium. Caveat: one regime + 2022, so Sharpes indicative. NET and fully reachable. Offline. |
| `run_zoo_overlay.py` | OpenAP zoo + FF total market | **The zoo as a market-neutral overlay — orthogonality finally pays, and it's implementable.** The capstone: relax the one constraint the prior two studies couldn't (long-only), and the zoo's value appears. Core = FF total market (Sharpe 0.50, full 1998-2024 overlap), overlay = levered EW zoo @10% vol, **corr −0.61**. Because the marginal hurdle for a negatively-correlated diversifier is *negative* (ρ·S_core = −0.31), the overlay helps until an annual cost drag of **~11.3%** — far above any realistic friction. At a realistic 4% cost a 30% overlay lifts the core Sharpe 0.50 → 0.69; the unconstrained optimum wants 100%. The implementable walk-forward test (36m trailing tangency dose, capped 50%, no future info) is the headline: even net of **6%/yr** cost it raises the equity core from Sharpe 0.46 / +9.1%/yr / −50% maxDD to **0.71 / +11.6%/yr / −42% maxDD** — higher return, higher Sharpe, AND shallower drawdown, out of sample. This is the precise resolution of the arc: the zoo's entire value is its market-neutrality, which is exactly what long-only access (Ideas 13-14) destroys; given the ability to short (or a liquid-alts sleeve), a 30-50% overlay is the reachable absolute-return upgrade the user was chasing. Caveat: requires market-neutral access; gross-zoo upper bound bracketed by the cost grid. Offline. |
| `run_factor_leadlag.py` | JKP 13 themes × 4 regions | **Do some factors LEAD others? Factor lead-lag network** (user prompt 2026-06-13). Tests whether fast market-priced factors lead slow accounting-priced ones — a directed network, distinct from own-momentum. Three layers with cross-region agreement as the kill test. **(A)** Net-leadingness ranking is ex-US-stable (developed/emerging/world_ex_us rank-agree 0.65-0.95) but the US is idiosyncratic (0.10-0.21); robust pattern = **size leads, profitability/quality/profit_growth lag**. **(B) The pre-declared FAST→SLOW hypothesis confirms in all 4 regions**: lag-1 corr +0.07 to +0.14 (t 1.6-4.0), while reverse slow→fast is always ~0 — the asymmetry proves genuine directed lead-lag, not shared contemporaneous exposure; construction-speed is the mechanism. **(C)** But the walk-forward cross-factor predictor, though gross-significant (t 2.9-4.4), dies at 20 bps, doesn't beat own-momentum cross-region, and never beats holding EW-ALL (Sharpe 1.1-1.5). Verdict: **mechanism yes (cross-region), edge no** — keep as a forecasting/risk fact, not a tradable sleeve. Offline. |
| `run_signal_lab.py` | 94 S&P-100 names | **IC lab for the per-stock BOCPD families** (Grinold & Kahn gate before the miner): monthly rank IC at 21/63/126d for `stock_regime_momentum` / `regime_age` vs momentum / reversal / low-vol baselines. First run: nothing passes t > 2 on this underpowered mega-cap sample; the per-stock gate trades IC magnitude for consistency (hit 0.58 vs 0.56, lower IC vol); low-vol's −0.03 IC is the survivorship bias made visible. Verdicts + tables in `docs/knowledge/research-roadmap.md` §5. Offline. |
| `run_retail_signal_lab.py` | 490 liquid US large-caps, 2010-2026 | **Retail-scale cross-sectional IC, net of Alpaca costs — loop iteration (stocks).** Tests the KB's untested levers on a 5×-breadth universe vs the Idea-1 S&P-100 lab: signal COMBINATION (z-score composite) and SECTOR-NEUTRALIZATION, with full Alpaca Large-Cap (4bp half-spread) netting and long-only/long-short decile books. **Structural null across 3 levers** (composite, sector-neutral, multi-horizon 21/63/126d): cross-sectional stock-selection IC is genuinely ABSENT here (momentum IC 0.001-0.002, |t|<0.2 at every horizon; low-vol negative — the survivorship/bull signature), so blending and neutralizing just sharpen noise. Sector-neutralization lifts hit rates (composite 0.49→0.54) but reveals no underlying signal. Incumbent retail book: long-only momentum top-decile nets Sharpe 1.15-1.17 / IR 0.40 vs EW-500 1.13, but at ~0 IC it's tail-driven, not a real signal. Verdict: HXZ confirmed — survivorship-biased large-caps are the wrong arena for cross-sectional alpha; the retail edge is not in single-name selection on this universe. Offline. |
| `run_retail_etf_lab.py` | 235 liquid ETFs, 2010-2026 | **The loop's IC improvement: cross-asset ETF rotation** — after single-name large-caps gave ≈0 IC, the loop moved to the ETF universe (cheapest Alpaca tier, 1bp, all shortable). Cross-sectional 12-1 momentum has a **genuine positive IC: 0.058 (t 2.40) at 21d, 0.089 (t 2.11) at 63d** — the first signal in the loop to clear t>2, net of Alpaca cost. **But it has decayed**: IC 0.09 (t 2.3) pre-2018 → 0.023 (t 0.78) post-2018 (direction stable, magnitude halved — post-publication crowding). Standalone long-only top decile loses to SPY (Sharpe 0.78 vs 0.97); the durable use is the **market-neutral L/S sleeve (corr −0.04 to SPY, +10.3%/yr net, Sharpe 0.52)** — a retail-implementable diversifier for the Idea-15 overlay, not a standalone winner. Composite blending again lost to the single best signal. Offline. |
| `run_orthogonal_ensemble.py` | FF mkt + OpenAP zoo + 235 ETFs, 2011-2024 | **THE BREAKTHROUGH — the orthogonal-alpha ensemble.** The program's capstone synthesis: it kept finding mutually-uncorrelated return streams and evaluating them in isolation. This assembles them. Three sleeves — equity core (FF total market), the levered EW zoo overlay (Idea 15), and the ETF-momentum L/S (Idea 17) — are **mutually orthogonal and stably so OOS** (corr core/zoo −0.44, core/etf +0.01, zoo/etf +0.03, holding in both sub-periods). A walk-forward inverse-vol risk-parity blend (10% vol target) lifts **Sharpe 0.93 → 1.70** net of cost, with LOWER drawdown (−20% vs −25%) and **+14% in 2022** (equity −20%) — the √N diversification of independent alphas the fundamental law guarantees, finally realized (naive equal-sum is worse at 1.28/−41% — risk-parity is the correct construction). Decisive bound via zoo-cost sensitivity: survives to **~12%/yr drag on the zoo leg** (realistic institutional 2-6%; Sharpe 1.31 even at 8%) before falling to equity parity. Honest caveats: the zoo is the workhorse (core+zoo alone = 1.56) and needs **market-neutral access** + is gross/survivorship-flattered OpenAP; the **retail-only** version (core+etfmom, no zoo) is just 0.96 — NOT a breakthrough; both alpha sleeves decayed post-2018 (recent half 1.27, still well above equity's 0.80). Offline. |
| `run_retail_alts_ensemble.py` | SPY + 235 ETFs + liquid-alt ETFs (BTAL/MNA/DBMF, MTUM/VLUE/QUAL/USMV/SIZE), 2011-2026 | **The breakthrough made retail-deployable — a zoo proxy you can trade on Alpaca.** Idea 18 needs a market-neutral sleeve the academic zoo gets by shorting hundreds of stocks (impossible retail). Tests two Alpaca-tradable proxies. **ZOO-B (beta-hedged factor ETFs)** FAILS — residual beta +0.20 and the post-2019 factor winter (Sharpe 0.41, corr decays to 0.02). **ZOO-A (liquid-alts basket BTAL anti-beta + MNA merger-arb + DBMF managed-futures, held LONG — funds short internally)** keeps the zoo's orthogonality cleanly: corr −0.42 to SPY, STABLE both halves (−0.38/−0.47), low standalone Sharpe 0.26 (a diversifier, not a return driver — the zoo's character). Winner: **risk-parity{SPY, ETF-momentum L/S, ZOO-A} → Sharpe 1.42 vs SPY 0.99**, maxDD −14% vs −24%, **+15% in 2022** (SPY −18%), and sub-period STABLE (1.48/1.42) — more robust than the institutional ensemble (which decayed 2.16→1.27) because managed-futures/anti-beta didn't fade like academic factors. Fully Alpaca-tradable; fund expense ratios already netted. The breakthrough survives retail — through liquid alts, not stock shorting. Network (yfinance) on first run for `datasets/retail_alts.csv`. |
| `run_retail_longonly_daily.py` | SPY/TLT/GLD/DBC/IEF + BTAL/MNA/DBMF/KMLM, DAILY 2009-2026 | **The breakthrough under hard retail constraints: LONG-ONLY, DAILY, NO leverage** (user 2026-06-13). Liquid alts stay (you BUY BTAL/MNA/DBMF — funds short internally); the ETF-momentum L/S is replaced by a long-only 200d-MA multi-asset trend sleeve (rotate to bonds/gold/cash in stress, no shorting); no vol-targeting up (cash account = fully invested). **Decisive lesson: the Idea-18 Sharpe jump was substantially a LEVERAGE+SHORTING effect — long-only no-leverage CANNOT raise SPY's Sharpe** (best ≈0.59-0.63 vs 0.62 full-sample). But the orthogonal diversification converts to DRAWDOWN & TAIL protection: 80/20 SPY/trend cuts maxDD −34%→−27% at ~SPY Sharpe; 60/20/20 with alts cuts the 2022 tail −18%→−11% and maxDD to −22%. And in the **post-2019 regime every diversified book beats SPY on Sharpe (0.70 vs 0.64)** with far lower drawdown — pure equity only won the 2009-2019 megabull. Naive inverse-vol weighting FAILS (over-allocates the −0.17-Sharpe alts sleeve); equity-dominant fixed weights are correct. The alts are crash insurance you buy long on Alpaca, not a Sharpe engine. Offline (uses cached retail_alts.csv). |
| `run_liquid_alts_basket.py` | SPY/IEF + wide liquid-alt ETF universe (managed futures, anti-beta, merger-arb, macro, return-stacked, tail), DAILY | **Are liquid alts a long-only way to add orthogonal returns? — YES, via MANAGED FUTURES** (user insight 2026-06-13). Characterized ~17 liquid-alt ETFs across categories. The standout class: managed futures (DBMF/KMLM/CTA) — uniquely uncorrelated to BOTH stocks (−0.10..−0.20) AND bonds (−0.34..−0.49), with crisis CONVEXITY (+0.8% in worst-decile equity months vs +0.1% in up months; +22-24% in 2022 when 60/40 had its worst year in a century). Adding a MF sled to SPY (long-only, no leverage, 2020-26) MONOTONICALLY improves everything: Sharpe 0.68→0.78, maxDD −24%→−14%, 2022 tail −18%→−7% — unlike BTAL (bleeds), MF have positive carry + negative corr + convexity, so more keeps helping. For 60/40 the lift is bigger (0.47→0.63, maxDD −21%→−7%). Honest counterweight: the strong modern funds are recent (DBMF 2019, KMLM 2020), and the full-cycle check (WTMF 2012-26, incl. the 2011-2019 MF drought) shows MF slightly LOWER Sharpe (0.78→0.76) while still cutting drawdown — the recent window flatters it. Verdict: managed futures are the retail, long-only, buy-it-on-Alpaca version of the zoo overlay — crisis insurance with positive carry, size ~10-15%, not a standalone Sharpe engine. Network (yfinance) on first run for datasets/liquid_alts_wide.csv. |
| `run_levered_alts.py` | QQQ/QLD/SSO/UPRO + managed futures (DBMF/KMLM/CTA), 2020-2026 | **'Better sauce' — leveraged equity + managed futures for more RETURN, long-only.** The user wanted to beat QQQ, not just smooth SPY. For a long-only investor that means leverage you BUY (leveraged ETFs) hedged by the crisis-convex MF sleeve. **Winner: QLD 60% + MF 40% — CAGR +24.3% (beats QQQ +21%), maxDD −35% (≈ QQQ −33%), Sharpe 0.84 (> QQQ 0.82)** — the 40% managed-futures cushions QLD's standalone −61% crash to −35% while 2× leverage lifts return above QQQ; rebalancing harvests the 2022 crash (sell the MF spike, buy the QLD dip). QLD 70/MF 30 gives +26.5% at −42%. Managed futures make leverage SURVIVABLE — that's the honest mechanism, not free alpha (risk-adjusted ≈ QQQ, scaled up). BIG caveats: 2020-26 is a short window flattering both the tech bull AND the 2022 MF bonanza; QLD's full-history maxDD is −81% (2008/2022) and the MF hedge only exists from 2019, so a 2008-style crash with MF failing is untested; leveraged daily-reset ETFs decay in choppy tape. An aggressive growth sleeve sized to stomach, not a free lunch. Network (yfinance) for datasets/levered_stacked.csv. |
| `run_alt_zoo_rotation.py` | 25-ETF liquid-alt zoo + SPY/QQQ/QLD, 2016-2026 | **A zoo of liquid alts + leadership rotation** (user 2026-06-13 — the session's synthesis). 25 alts across 11 categories (managed futures, anti-beta, merger-arb, macro, long/short-vol, options-income, return-stacked, commodities, gold, converts); each is one packaged orthogonal sleeve, so rotate into the LEADERS by trailing-6m momentum instead of holding all. **Rotation works: top-8 momentum runs Sharpe 0.47 / +7.8%/yr vs the static EW zoo's 0.30 / +5.5%, and caught +18% in 2022 vs the static +1%** — the user's idea is validated, timing the alt zoo beats holding it. BUT even the rotated alt sleeve is a low-Sharpe (0.35-0.47) diversifier, so it can't out-RETURN QQQ. The return-hungry combo: QLD 60 + rotated-alt 40 BEATS QQQ on return (+28.3% vs +24.7%) and the rotated zoo makes the leverage far more survivable (maxDD −37% vs QLD-alone −59%) — but at Sharpe 0.97 < QQQ's 1.06. **The wall: over 2016-26 nothing in the long-only liquid-alt + leverage toolkit beats QQQ's Sharpe 1.06**; leverage buys more RETURN (rotated alts keep it holdable), not more risk-adjusted return. Network (yfinance) for datasets/alt_zoo.csv. |
| `run_unified_zoo_rotation.py` | one zoo: 25 alts + SPY/QQQ + QLD/SSO/UPRO/TQQQ, 2016-2026 | **The user's capstone idea: put leveraged equity INTO the zoo and let leadership rotation pick everything.** Dual momentum (200d trend filter + 6m relative strength), top-k, managed-futures default for empty slots; tested vs alts-only and alts+equity menus, plus vol-targeted variants. Result: putting LEVERAGED equity in the rotation captures QQQ-like RETURN (+19.9%) and nails the 2022 SLOW bear (−4% vs QQQ −33% — the rotation genuinely fled to managed futures), BUT Sharpe 0.63 < QQQ 0.86 at maxDD −54% vs −33%. The adaptive rotation protects slow bears but momentum exits FAST crashes late (held leverage into COVID-2020) and leveraged ETFs decay in whipsaws; vol-targeting (15/20%) doesn't fix it (the vol estimate lags too). Clean negative — adaptive-leverage rotation is a slow-bear hedge, not a QQQ-beater. Confirms the session's wall: no long-only retail toolkit (diversification, liquid alts, rotation, leverage, or adaptive rotation over a unified zoo) beats QQQ's risk-adjusted return over 2016-26. Offline (cached). |
| `run_daily_leading_signal.py` | QQQ/QLD + VIX term structure + managed futures, DAILY 2011-2026 | **Breaks the wall: DAILY + a LEADING vol signal beats QQQ.** The user's two fixes to the failed monthly rotation — (1) rebalance DAILY not monthly, (2) use a LEADING signal (vol spikes at stress ONSET, before momentum/trend breaks). De-risks leveraged equity (QLD, 2x) into managed-futures-or-cash when the signal flags stress, daily, net of 10bp switch cost. Just moving the lagging 200d-MA trend signal to DAILY lifts it from Sharpe 0.63 (monthly) to 0.82. **The winner is fast REALIZED VOL (20d): de-risk when it spikes >1.3-1.5x its 1y median -> CAGR ~30-32% (vs QQQ 19%), Sharpe ~0.9-0.98 (vs QQQ 0.75), maxDD -42% (vs QLD-alone -64%), robust across the central window/threshold grid AND both sub-periods (Sh<=18 0.79-0.93, Sh>18 0.99-1.05)** — and it holds with plain CASH as the safe asset, so it's not reliant on the recent MF funds. Mechanism: leveraged ETFs decay MOST in high vol, so exiting on the vol spike dodges both the decay AND the crash; daily speed catches fast crashes (COVID close-up: VIX-TS signal +3%/-21% vs QQQ -29%, lagging trend -43%). VIX-TS alone whipsaws (Sharpe 0.75); realized vol is the clean version. REVISES the session wall (monthly couldn't beat QQQ; daily+leading-vol does). Caveat: still -42% maxDD (2x leverage); 2011-26 has COVID/2022 in-sample, mitigated by parameter + sub-period robustness. Network (yfinance) for VIX. |
| `run_vol_managed.py` | QQQ/QLD + managed futures, DAILY 2011-2026 | **More return WITHOUT estimating returns — pure volatility management** (user 2026-06-14: returns are noise, strip out the math). The only input is EWMA volatility (predictable — it clusters); the compounding math does the rest via VARIANCE DRAIN: geometric growth g ~ mu - sigma^2/2, so stabilising vol lifts compound growth with no return forecast. Continuous exposure = clip(target_vol / EWMA_vol(QQQ), 0, 1) in QLD (2x), rest in managed-futures/cash, daily. Quantifies leverage decay: QLD bleeds -4.4%/yr to variance drain (9x QQQ's -0.5%). **Vol-managed QLD @30% compounds at GEO +25.7%/yr vs QQQ +19.0% at the SAME -35% maxDD; @25% beats QQQ's return at LESS drawdown (-30%)** - Sharpe ~0.80 vs 0.75. Purest 'no-estimation' lever: smoother and parameter-light vs the binary fast-vol signal (higher Sharpe 0.9-0.98 but has a threshold). 5-panel dashboard: equity curves, variance-drain decomposition, stabilised rolling vol, the exposure dial, drawdowns. The mechanism behind the whole session's leverage results - manage vol (predictable), not returns (noise). Offline. |

### PEAD (Post-Earnings-Announcement-Drift) — run in order

| script | what it does |
|--------|--------------|
| `download_finance_data.py` | One-time: download the FinanceDatabase equity universe → `financial_data/`. Requires `pip install -e .[pead]`. |
| `run_pead.py` | Stratified-sample the universe, fetch earnings+prices (checkpoint/resume), build the SUE panel, run Fama-MacBeth IC tests overall + per cap bucket. Writes `results/pead/signal_panel.csv`. |
| `walk_forward_pead.py` | Walk-forward long-short validation on the panel produced by `run_pead.py`. |

`main.py` and the PEAD runners require network access (yfinance / FinanceDatabase);
the other portfolio studies run offline against the bundled `datasets/` files.

`_bootstrap.py` is a shared shim, not a study — import it first in every script.
